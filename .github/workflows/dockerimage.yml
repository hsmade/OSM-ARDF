name: Docker Image CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    - name: Build and upload image
      env: 
        super_secret: DOCKER_TOKEN
      run: docker login -u hsmade -p "${DOCKER_TOKEN}" && make upload
